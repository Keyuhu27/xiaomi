ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxx

# 部署到 Railway 这类公网环境时务必设置，不然任何拿到 URL 的人都能进来问
# 数据、消耗你的 API 额度。本地开发不设置这个变量就不会要求输入密码。
APP_PASSWORD=

# 数据库文件存放目录。本地开发不用管，用默认的 data/ 目录就行；部署到
# Railway 时指向挂载的持久化 volume（比如 /data），不然每次重新部署数据
# 都会被清空。
# DATA_DIR=/data
